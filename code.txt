
AGENT person 1 {

    VARIABLE distance: 0 = IF infected THEN distance ELSE distance + 1;

    CONST infDistance = 5;
    CONST infTimespan = 7;

    DYNAMIC infected = distance >= infDistance;
    DYNAMIC remainingDays = IF infected THEN remainingDays - 1 ELSE infTimespan;
}
agent person 100 {
    const offset = 1;

    property people = agents(person);
    property distance = sum(people => p => p.offset);

    property x = distance;
    property y = 100;

    const coloured = false;
}